<?xml version="1.0" encoding="utf-8"?>
<!-- Shared with androidx.core.content.FileProvider (audit W1.5):
     verified update artifacts are downloaded into the app cache dir
     (path_provider getTemporaryDirectory) before install. -->
<paths>
    <cache-path name="update_cache" path="." />
    <files-path name="update_files" path="." />
</paths>
