# Progress — 2026-09-01 — Toolchain install + platform scaffold + spec version fix

> Session: dependencies, versions, scaffold. Compares vs `SPEC.md:Tech Stack` + `tasks/todo.md`.

## What was done

### 1. Researched and corrected the toolchain version (web verified)
- **Finding:** `Flutter 3.41.9` DOES exist as a git tag (`00b0c91f...`), so the spec pin was not a typo. But the spec's own package stack no longer works with it:
  - `material_3_expressive` **1.x** (the full 45-widget M3E set) requires **Flutter ≥ 3.44 / Dart ≥ 3.12** and needs the decoupled `material_ui` package. The spec's `^0.1.0` pin is a stale pre-1.0 beta.
  - `flutter_foreground_task` **11.x** (current) also requires **Flutter ≥ 3.44 / Dart ≥ 3.12**, needs Kotlin 2.2.20+ and Gradle 8.11.1+.
  - `cue` latest is **0.3.1**; the spec's `cue: ^1.0.0` does not exist → would break `pub get`.
- **Android 7 support confirmed:** Flutter's minimum Android SDK is **API 24 (Android 7)** since Flutter 3.35 (supported grid `24 to 36`). Any modern Flutter + the app's `minSdk = flutter.minSdkVersion` (24) satisfies Android 7+.
- **Chosen:** **Flutter 3.47.2** (latest stable, 2026-08-26) + bundled **Dart 3.13.2**. This is the best modern version that still supports Android 7 (API 24) and unlocks the required modern packages. Installed to `C:\tools\flutter`.

### 2. Installed the full toolchain
- Flutter 3.47.2 → `C:\tools\flutter` (confirmed by `local.properties` `flutter.sdk` already pointing there).
- `flutter pub get` → resolved **154 packages**; lockfile rebuilt (1223 lines).
- Android SDK upgraded **API 35 → 36** + `build-tools;36.0.0` via sdkmanager (Flutter 3.47 requires compileSdk 36). NDK **28.2.13676358**, **JDK 17** already present.
- Go **1.26.5** present; `gomobile`, `gobind`, `golangci-lint` present in `C:\Users\qmahyar\go\bin`.
- `go/amnezia-box` dependencies downloaded (`go.sum` 613 lines). **Note:** no `submodules/` dir — the `hoaxisr/amnezia-box` fork wires AmneziaWG via go.mod `replace` (`hoaxisr/amneziawg-go v3.1.0-awgm.2`), not a wireguard-go submodule.
- `flutter analyze` → **No issues found**.

### 3. Scaffolded missing platform dirs
- `flutter create --platforms=android,windows --project-name yourvpn --org com.yourvpn .`
- Produced `android/` (build.gradle.kts, settings.gradle.kts, gradle wrapper, AndroidManifest, res), `windows/` (runner + CMake), `.metadata`, README.md.
- `android/app/build.gradle.kts` uses `minSdk = flutter.minSdkVersion` (24 = Android 7), `compileSdk = flutter.compileSdkVersion` (36), namespace/applicationId `com.yourvpn.yourvpn`.
- Removed the generated default counter `test/widget_test.dart` (app is not a counter).

### 4. Spec/docs version alignment
- Updated `3.41.9 → 3.47.2` across AGENTS.md, SPEC.md, goal.md, intent.md, research/audit.md, tasks/plan.md.
- `SPEC.md`: `Dart 3.9 → 3.13`, `Android SDK 34 → 36`, noted `material_ui` 1.1.0 requirement, added `platforms;android-36` + `build-tools;36.0.0` to the CI sdkmanager line.
- `AGENTS.md`: `Pin flutter 3.47.2`.
- `pubspec.yaml`: fixed broken pins — `cue ^1.0.0 → ^0.3.1`, `material_3_expressive ^0.1.0 → ^1.1.1` (+ `material_ui ^1.1.0`), `sdk '>=3.12.0'`, `flutter '>=3.44.0'`, and bumped drift/foreground_task/etc. (see `pubspec.yaml`).
- Left third-party research files (e.g. `01-Karing.md`) untouched.

## One transient issue
- During `flutter create`, pub.dev returned a transient **"authorization failed"** and clobbered `pubspec.lock`. Recovered by `dart pub get --offline` (cache was warm) → 153 deps restored, lockfile rebuilt.

## Blockers / evidence
- `flutter analyze` clean; `flutter doctor` Android toolchain green (SDK 36).
- Root `go.mod` still has a **stale `replace github.com/sagernet/wireguard-go => ./go/amnezia-box/submodules/wireguard-go`** (path does not exist). Fix when building core `libbox` (todo:2) — the fork owns the awg replace internally.
- AndroidManifest is default; needs VPN permissions (`INTERNET`, `FOREGROUND_SERVICE`, VpnService, etc.) in the tunnel/platform todo.

## Next
- todo:2 Core real bind (libbox.aar via `make -C go/amnezia-box lib_android`) + fix stale root `go.mod` replace + pin fork SHA.
- todo:3 Tunnel 03 Deep (`lib/core/services/tunnel.dart`).

---

# Progress — 2026-09-01 (session 2) — Core done: libbox.aar with_awg + sing-box.exe Windows

## What was done (todo:2 Core)

### 1. Root `go.mod` repaired
- Removed stale `replace github.com/sagernet/wireguard-go => ./go/amnezia-box/submodules/wireguard-go` + orphan `require sagernet/sing-box` (no root Go code exists; fork owns the AWG wiring via its own go.mod `replace hoaxisr/amneziawg-go/v3 v3.1.0-awgm.2`).

### 2. Fork pinned + built with `with_awg`
- Fork verified at tag **`1.14.0-rc.1-awgm.15`**, SHA **`57276220a20679cad762c9644f6abdaf39ab7688`** (matches the AGENTS `awg-1.14-rc1` intent).
- Key discovery: the fork's official `cmd/internal/build_libbox` does **NOT** pass `with_awg` (would build the stub that returns `Awg is not included`). Our build mirrors its tag set + adds `with_awg`: `with_gvisor,with_quic,with_wireguard,with_utls,with_naive_outbound,with_clash_api,with_usbip,with_openvpn,with_openconnect,badlinkname,tfogo_checklinkname0,with_tailscale,ts_omit_*,with_awg`.
- Installed **sagernet/gomobile v0.1.13** (golang.org/x/mobile gomobile lacks `-libname`/`-tags` bind flags).
- Recipe in root `Makefile` (canonical, CI target) + `scripts/libbox.ps1` (Windows-local twin, .NET `ProcessStartInfo.ArgumentList` quoting; `cmd.exe` spawns `=E:` env vars that panic Go). javapkg `com.yourvpn`, libname `box`, androidapi 24, version-stamped ldflags.

### 3. Artifacts + evidence
- `android/app/libs/libbox.aar` **123.9MB**, 4 ABIs (arm64-v8a 85.2MB / armeabi-v7a / x86 / x86_64), classes `com.yourvpn.libbox.*` (210 entries).
- Strings check on arm64 `.so`: **751** `amneziawg-go` hits, `sing-box/transport/awg` + `protocol/awg` present, stub string `Awg is not included` **ABSENT**.
- `go test -tags "...,with_awg" ./option/... ./common/srs/...` → ok.
- `awg` endpoint (type `awg`, Jc/H1-H4/S1/S2 params) passes `sing-box check` at runtime — the definitive stub-vs-real proof.
- `libbox-legacy.aar` (API 21) **skipped**: minSdk is 24; legacy is dead weight.

### 4. Windows pivot: sing-box.exe (not dll)
- `libbox` is a library package → `-buildmode=c-shared` needs a main wrapper; upstream has no dll, clients run the CLI as a subprocess. Built `windows/sing-box.exe` v1.14.0-rc.1-awgm.15, tags `...,with_awg,with_purego` (purego needed for cronet on Windows), CGO disabled. App will talk to it via command server / clash API.

### 5. Android build integration
- `android/app/build.gradle.kts`: `implementation(files("libs/libbox.aar"))`, `minSdk = 24`, `compileSdk = 37` (flutter_secure_storage requires 37), ndkVersion pinned 28.2.
- `android/gradle.properties`: `kotlin.incremental=false` (Windows Kotlin daemon "could not close incremental caches" flake).
- `flutter build apk --debug --target-platform android-arm64` → **green** (`app-debug.apk`).

## Blockers / notes
- No `make` on this Windows box → use `pwsh scripts/libbox.ps1 main|windows`; CI (Linux) uses the Makefile, same args.
- gomobile exit code 1 after aar is written is a post-jar warning; artifact verified complete.
- WinLibs UCRT gcc installed via winget (for any future cgo need); not required for the CGO_ENABLED=0 exe.

## Next
- todo:3 Tunnel 03 Deep — `lib/core/services/tunnel.dart` consuming `com.yourvpn.libbox.*` (Android) + `sing-box.exe` subprocess (Windows).

---

# Progress — 2026-09-01 (session 3) — todo:3 Tunnel 03 Deep DONE

## What was done
- lib/core/services/tunnel.dart: deep Tunnel module. connect(tag) -> TunnelState runs grant → battery → foreground → airplane → resolve → tor check → establish → protect → box.start → firewall.enforce; every failure path funnels into _block(reason) = firewall.enforce + fd close + TunnelState.blocked. disconnect() reverses: box.stop → closeFd → foreground.stop → firewall.relax. Box crash while connected → blocked. Idempotent connect/disconnect guards. status broadcast stream.
- Adapters as interfaces: PlatformAdapter (permission/battery/airplane/establish/protect/closeFd), ForegroundAdapter, BoxAdapter (start(TypedConfig)/stop/events incl. crashed), FirewallAdapter, TorAdapter (SOCKS 127.0.0.1:9050 probe), ConfigSource (tag→TypedConfig; JSON never crosses the seam).
- MethodChannelPlatformAdapter on channel pn_service, safe defaults on MissingPluginException (permission=false, airplane=true, establish=null) so no caller can skip the guard sequence before Kotlin lands.
- lib/modules/vpn/logic/vpn_notifier.dart: VpnNotifier (manual Notifier — riverpod_generator not in deps, YAGNI) + sealed-shape VpnState{phase, tag, blockReason}; 	unnelProvider overridden in tests/main. Calls Tunnel.connect("HKG-02") — tag only, no JSON.
- Kotlin thin: ForegroundService.kt (dataSync|remoteMessaging, START_STICKY, channel "vpn_status", tap→MainActivity), BatteryOptHelper.kt (isIgnoring + REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, VPN-acceptable use). Manifest: INTERNET, FOREGROUND_SERVICE(+DATA_SYNC|REMOTE_MESSAGING), POST_NOTIFICATIONS, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, service declared. No VPN logic in Kotlin.
- Tests 	est/tunnel/ (15, all green, no device): shared-log ordering grant→battery→foreground→establish→protect→start→firewall, airplane→block (no establish), tor-down→block (box never starts, #4200), permission-denied→block, establish-null→block, box-start-fail→closeFd+block, unknown-tag→block, disconnect reversal order, disconnect no-op, connect idempotent, status transitions, box-crash→block, battery request fires non-fatally, notifier tag pass-through x2.
- Evidence: lutter test test/tunnel/ 15/15; lutter analyze 0 issues; lutter build apk --debug green with Kotlin + manifest changes.

## Notes
- Dart lesson: redirecting const constructors cannot have field initializers — switched VpnState to factories; named params cannot assign to private fields (prefer_initializing_formals) — adapters are public final fields.
- go/libbox/bridge.go from the todo files list: N/A — fork's xperimental/libbox is already the typed Go bridge; the aar javapkg com.yourvpn carries it.

## Next
- todo:4 GeoAsset 05 Deep (geo_asset.dart ensure(tag)/refreshAll + 6h cache + initial_path .srs) then todo:5 Ingestion 02, todo:6 Routing 01.

---

# Progress — 2026-09-01 (session 3 cont.) — todo:4 GeoAsset + todo:5 Ingestion DONE

## todo:4 GeoAsset 05 Deep
- lib/core/network/http_cache.dart: ETag + rate-limit aware cache. 403+x-ratelimit-remaining:0 → GitHubRateLimitException(resetAt), 429 → RateLimitException(retryAfter), 304 → touch, network death → stale body. Injectable clock for tests. Disk + memory tiers.
- lib/modules/geo/geo_asset.dart: ensure(tag) always succeeds (cache → bundled initial → download), getPath(tag) sync for dns/routing builders, ruleSetEntries() = remote binary + download_detour PROXY + update_interval 24h, refreshAll() 6h stale-while-revalidate, rate limits rethrown, other failures keep stale bytes.
- ule_sets/initial_assets/geosite-cn.srs (55.5KB) + geoip-cn.srs (34KB) vendored from SagerNet rule-set branches; sing-box rule-set decompile passes on both.
- 12 tests: initial fallback, no-HTTP when cached, download path, unknown tag, stable getPath, entries shape, GitHubRateLimitException, RateLimitException, stale-keeps-bytes offline, 6h freshness skips HTTP, If-None-Match + 304 + changed-200 (revised mock bodies), real SRS magic bytes.

## todo:5 IngestionAdapter 02 Deep
- 
ormalized_endpoint.dart: sealed union WireGuardEndpoint(+WireGuardPeer+AwgValues) | VlessEndpoint | VmessEndpoint | ShadowsocksEndpoint | TrojanEndpoint | Hysteria2Endpoint | TuicEndpoint. AwgValues (Jc/H1-H4/I1-I5/Id/Ip/Ib) lives in lib/utils/amnezia_values.dart (presets + generateRandom with H-collision-free, S1+56!=S2, Jmax<1280).
- parsers.dart: ClashYamlParser (proxies+listeners, amnezia-wg-option), SingboxJsonParser (outbounds+endpoints incl. awg type, SIP008-ready), Vless/Vmess/SS/Trojan/Hysteria2/Tuic URI parsers, WgIniParser (INI with Amnezia keys). IngestionAdapter content-sniffs (wg INI → yaml → json → share links, base64 bundle unwrap) + 6h parse cache per URL.
- Key bug fixed: Uri.parse lowercases host, destroying base64-in-host (vmess/ss whole-link) — manual parsing now; verified via dart repro script.
- 16 tests covering 7 formats + bundle + malformed-skip + cache identity. Suite total 43 tests, analyze clean.

---

# Progress — 2026-09-01 (session 3 cont.) — todo:6 RoutingCompiler 01 Deep DONE

## What was done
- lib/modules/routing/routing_policy.dart: typed RoutingPolicy + RouteRule (30-field surface as typed fields: domains/domain_suffix/domain_keyword/domain_regex/geosite/geoip/ip_cidr/source_ip_cidr/port/source_port/network/protocol/package_name/process_name/wifi_ssid/wifi_bssid/user/inbound/clash_mode/rule_sets + logical or/and + nested rules + invert; outbound nullable, required top-level, forbidden on sub-rules).
- lib/modules/routing/routing_compiler.dart: compile(policy) -> CompiledRoute {rulesJson, ruleSetTags (deduped), validationErrors (all at once)}. Logical rules emit fork-native 	ype: logical (not legacy logical:true). Unknown rule_set tags → error, not throw. Empty condition rule → error. Invert passthrough.
- lib/modules/routing/config_assembler.dart: thin envelope builder (log/dns/inbounds tun/endpoints|outbounds/route) — WG/AWG endpoints go to ndpoints (1.14), selector PROXY + auto_detect_interface + final.
- GeoAsset.ruleSetEntriesFor(tag) added for per-tag entry emission.
- 9 tests: happy path shape, tag dedup, unknown-tag error, empty-rule error, multiple-errors-at-once (editor shows all), sub-rule-outbound error, **real sing-box check -c on compiled config passes**, rule_set entries shape, real geosite-cn.srs decompile round-trip.
- Fork-schema fixes found by real check: DNS servers new format (type: https + server:, not address URL), no dns.rules outbound item (deprecated), logical rules 	ype: logical.

## Suite status
- 52 tests total (tunnel 15 + geo 12 + ingestion 16 + routing 9), analyze clean.

---

# Progress — 2026-09-01 (session 4) — todo:7 DNS + FakeIP DONE

## What was done
- lib/modules/dns/dns_config.dart: 1.14 typed servers (dns-proxy https 1.1.1.1 detour PROXY / dns-local / dns-fakeip inet4_range 198.18.0.0/15 + inet6 fc00::/18), dns.rules (geosite-cn→local, clash_mode Direct/Global, A/AAAA→fakeip), final dns-proxy, fake-ip filters (+.lan +.local ntp), hijackRules() protocol:dns→action:hijack-dns.
- ConfigAssembler: dns block + hijack first in route.rules + fakeip pool in tun address + geosite-cn guaranteed in rule_set tags. dns.fakeip legacy key REMOVED (fork FATALs on it).
- 	est/dns/ 5 tests: shape, hijack, filters, **real sing-box check on full DNS+FakeIP config**, pool-in-tun address.
- 	ool/gen_profile.dart generates profiles/config.wg-awg.json from real modules (not hand-written); sing-box check -c profiles/config.wg-awg.json → exit 0.
- Suite: 57 tests, analyze clean.

---

# Progress — 2026-09-01 (session 4 cont.) — todo:8 AWG Domain 04 Deep DONE

## What was done
- lib/modules/vpn/amnezia/awg_config.dart: AwgConfig.validate() reports ALL violations at once (key 32B base64, endpoint host:port, addresses, Jc 0-128, Jmin<=Jmax<MTU, S ranges, S1+56!=S2, H1-H4 pairwise distinct = H-intersect-H2-empty). toEndpointJson() emits fork-native endpoints[] entry. AwgProfile enum (quicMimic/balanced/stealth) with .label for UI, .fromPreset(), .generateRandom() (100-draw invariant test). amnezia_values.dart refactored: AwgPreset + AwgPresetValues extension (no duplicate enums), generateRandom() secure-random H-collision-free S1+57!=S2.
- 	est/awg/ 10 tests: 3 presets validate, header-distinct invariant across presets, 100 random draws hold all invariants, validator catches collision/S1+56/Jmax>=MTU/short key/bad endpoint, **real sing-box check on toEndpointJson output passes**.
- Suite: **67 tests, analyze clean**. todo:7+8 complete = 8/17.

## Next
- todo:9 platform thin adapters + wizard (VpnServiceBridge.kt establish/protect handlers for the vpn_service channel — Dart side already shipped in todo:3).
- todo:10 UI bento home (big; fresh context recommended).

---

# Progress — 2026-09-01 (session 5) — todo:9 Platform + Wizard DONE

## What was done
- YourVpnService.kt: real VpnService (BIND_VPN_SERVICE, intent-filter android.net.VpnService) — establish() with TUN 172.19.0.1/30 + fdfe:dcba:9876::1/126 + DNS 172.19.0.1 + 0.0.0.0/0 + ::/0 routes, MTU 1408, configure intent to MainActivity; fd registry for closeFd; static callback handoff for the async establish.
- VpnServiceBridge.kt: vpn_service channel handlers (isVpnPermissionGranted, requestVpnPermission startActivityForResult VpnService.prepare, isIgnoringBatteryOptimizations, requestIgnoreBatteryOptimizations, isAirplaneMode, establish, protect, closeFd, startForeground, stopForeground). MainActivity wires configureFlutterEngine + onActivityResult.
- Manifest: YourVpnService declared with permission + intent-filter.
- Dart PlatformAdapter: requestVpnPermission() added (interface + MethodChannel adapter + test fake).
- lib/modules/onboarding/wizard.dart: 3-step wizard controller (WizardController riverpod Notifier: vpnPermission -> batteryExemption -> perAppSplit -> done, back navigation, allowlist/bypass mode, app toggle) + Wizard widget (Card, SegmentedButton, CheckboxListTile, l10n via AppLocalizations).
- l10n: 14 new keys EN + FA (wizardStepOf with placeholder, permission/battery/per-app strings), gen-l10n configured synthetic-package:false output-dir lib/l10n.
- 6 wizard tests (permission grant advances, battery request fires, skip without request, toggle/allow-mode/finish, back navigation).
- Suite: **73 tests, analyze clean, flutter build apk --debug green** (Kotlin compiles). 9/17 todos done.

## Next
- todo:10 UI bento home (big, fresh context recommended) or todo:11 Health 06.

---

# Progress — 2026-09-01 (session 6) — todos 10-17 DONE → 17/17

## What was done
- **todo:10 UI:** go_router ShellRoute 5 tabs, bento home adaptive 4/2/1 columns, PowerTile wired to real VpnNotifier, StateTile with blockReason, Semantics + EdgeInsetsDirectional. main.dart wires real MethodChannelPlatformAdapter.
- **todo:11 Health:** lib/modules/health/health.dart — Health.snapshot() 0-6 hierarchical (hijack=2 + ping=2 + stability>=80% over >=2-sample window=2), FakeIpDnsProber (198.18/15 detection), TcpPinger. 6 tests.
- **todo:12 Updates:** lib/modules/updates/updater.dart — UpdateFetcher (platform-filtered assets, 403/429/401 typed errors), UpdateInfo semver compare, latestJsonFromRelease platform map (flutter_server_box style). Reuses HttpCache Fetch seam. 8 tests.
- **todo:13 Sec:** FirewallPolicy (hijack-dns first, pinned DNS DIRECT, ipv6 BLOCK ::/0, lan BLOCK, enforce-on-connected/blocked semantics) + scripts/leak_test.sh (7 scenarios, tcpdump-based). 5 tests.
- **todo:14 Release:** .github/workflows/build-android.yml (gomobile+NDK+strings sanity+split APKs+signing+latest.json+sha256), build-windows.yml (CGO off exe+zip+hash), build-linux.yml stub.
- **todo:15 Website:** docs/site/index.html (self-contained landing, latest.json platform-filtered download buttons) + gh-pages.yml.
- **todo:16 Upstream:** upstream-check.yml (weekly drift report → bump PR flow per decisions.tsv).
- **todo:17 Tracking:** this file + decisions.tsv + todo.md checkboxes + handoff refresh.

## Final state
- **92 tests, flutter analyze clean, sing-box check -c profiles/config.wg-awg.json exit 0, apk debug build green.**
- Known deferrals (documented, not silent): M3E widget-level polish + 200% goldens (todo:10 note), leak tests need real device, wizard sample app list static until installed_apps plugin.

---

# Progress — 2026-09-01 (session 7) — 8-way parallel review + critical fixes

## Review verdicts (8 subagents, all areas)
- Engine/Build PASS (1 major CI drift), Tunnel PASS (races found), Geo PASS-with-caveats (epoch bug), Ingestion PASS-with-gaps (base64 alphabet, multi-peer, fuzz), Routing PARTIAL (1.12-removed fields, ports as strings), DNS PASS-with-gaps, AWG PASS, Platform PARTIAL (callback races), UI PASS-with-gaps (health RangeError, hardcoded labels), Updates PARTIAL (no UI wiring), Sec PARTIAL (script bugs), Release FAIL (parse blocker), Website/Upstream PARTIAL.

## Fixed this session
1. [CRITICAL] build-android.yml ${{ V }} invalid expression → github.ref_name; signing step moved BEFORE build; latest.json attached to release; permissions: contents: write added.
2. [HIGH] build-windows.yml tag drift (with_clash_api + with_usbip restored) + permissions block.
3. [HIGH] health.dart RangeError at score 6 → band-mapped level getter.
4. [HIGH] http_cache.dart x-ratelimit-reset parsed as epoch timestamp (was relative).
5. [HIGH] routing compiler: geosite/geoip fields (removed in 1.12) now compile to rule_set tags; port/source_port emitted as uint16 ints; BLOCK outbound → native action: reject; assembler: selector always has DIRECT member + DIRECT outbound + endpoints kept when awg; route.default_domain_resolver added (deprecation FATAL).
6. [HIGH] ingestion: decodeBase64Flex (standard alphabet first, url-safe fallback); WG INI rewritten section-aware (multi-peer supported, allowed_ips per-peer); intField/stringField coercions so fuzz → FormatException never TypeError; loadYaml wrapped.
7. [HIGH] tunnel.dart: epoch counter (disconnect-during-connect no longer races; stale sequences abort), foreground.start/firewall.enforce guarded → block instead of stuck connecting.
8. leak_test.sh DNS-leak grep now catches ANY phys port-53 (was checking for tunnel IP); split-audit subnet 172.19.0; wizard Directionality.of(context) (FA RTL).

## Final state
- **96 tests, analyze clean, sing-box check on regenerated profile exit 0.**

## Open findings (documented, next session)
- VpnServiceBridge: establish callback queue + orphaned-fd guard (Kotlin, needs device test).
- Profile peer key in fixtures is 64-hex (runtime key parse); Id/Ip/Ib validation absent; 24/30 fields; groups/3-tier seam unbuilt.
- Updates module not wired into UI; latest.json single-producer race between workflows; site hardcoded repo slug.
- fake-ip-filter-mode: fork's fakeip server type has no filter field — documented limitation.
