# Progress 2026-09-04 — roadmap P2 sweep: updates fail-closed, urltest, transports, backup, metrics tile

Mother-of-all-sessions run: scout workflow (6 readers + critic) → clusters A–E inline → adversarial review workflow (5 dims + per-finding skeptics) → trackers. Baseline 251 tests, clean tree at `790301d`.

## Scout workflow (7 agents, 466k tokens)

- fork-structs probed the real engine: SSH option struct + key formats (OpenSSH-PEM/PKCS8/PKCS1 OK; ssh-keygen PKCS8-export output FAILS `ssh: no key found`), xhttp `x_padding_bytes` MANDATORY (fork struct no omitempty; absence FATALs `cannot be disabled`), ECH probed (no-config OK via DNS HTTPS-records; legacy pq fields FATAL), urltest idle_timeout/interrupt_exist_connections exist engine-side, ssh outbound-only (endpoints[] rejects).
- parsers scout mapped all 9 parsers, the 5 exhaustive switch sites (+ endpoints_screen import = 6th), transport fields parsed-then-dropped, and the EndpointStore swallow-all wipe hazard.
- urltest scout found the two-emitter split (compiler vs `ProfileConfigSource._groupJson` — the real connect path) and that `resolve(tag)` ignores tag content.
- backup scout: 5 persisted files, `~/.yourvpn/` env-root, NO argon2 dep in tree, flutter_secure_storage declared-but-unused, merge-by-tag precedent.
- updates scout confirmed Dim-9 bugs still open; critic caught a compile-break in cluster A's pending diff (missing `http:` arg) + 4 semantic fixes (empty-localVersion semantics, x86 mapping, mirror on manual path, HttpCache disk backing).

## Fixed (clusters A–E, all gated per-commit)

1. **Updates-wiring fail-closed** (`cf92abf`): epoch reset parse; StoredUpdate envelope (restart no longer shows Available forever); defaultMirrorUrl on both check paths; bg task real localVersion via inputData + policy `.update`; resolvePlatformKey fail-closed (unknown ABI/x86 throw); geo production wiring — `_seedGeoAssets` deferred startup, rootBundle bundledLoader (bundled fallback actually works on Android now), tag-keyed refreshAll, disk-backed ETag cache. +pubspec asset declaration.
2. **urltest best-node** (`c7f2e59`): form fields (url/interval/tolerance were silently dropped), `_groupJson` interval>0 guard, urltest group resolves to GROUP tag at selection (engine picks best + fails over), `latency.dart` TCP sweep + measure button + best-ms subtitle.
3. **Transports parity** (`c4e83ac`): SshEndpoint (ssh:// URI + sing-box JSON import, emit per probed struct), TransportOptions (ws/grpc/httpupgrade/xhttp/http) with `_resolveTransport` precedence chain, ECH in `_tls`, full transport detail capture on sing-box import, 6 exhaustive switch sites updated. Every shape round-tripped through the REAL `windows/sing-box.exe check` (test/p1 ×18).
4. **Encrypted backup** (`1486dde`): BackupService envelope over 4 stores; OWASP Argon2id (m=19MiB t=2 p=1) + AES-256-GCM; wrong-pw fail-closed; picker-writes flow (file_picker 12.x bytes API); replace + merge-by-tag import; endpoints-screen UI. Deps added via pub.flutter-io.cn mirror (pub.dev 403 on this network).
5. **Metrics tile + goldens** (`cf4201b`): StatsTile reads real ConnectMetrics; `_wrap` clamp bypass so the 2.0 golden proves RAW layout; stats golden added; PowerTile/StateTile 23px overflows fixed (caught by the pre-clamp probe — the test now proves something).

## Review workflow

Ran post-clusters: 5 dimension reviewers (correctness-emitters, concurrency-state, security-crypto, ui-store-wiring, test-verify-gaps) → adversarial skeptic per finding. Results + fixes → see handoff.md and final commit.

## Verify

- `flutter test --no-pub` → **301 pass** (251 baseline + 50 new)
- `dart/flutter analyze` → No issues
- `windows/sing-box.exe check -c profiles/config.wg-awg.json` → exit 0 (per prior gate; all new emitter shapes individually check-probed in tests)
- `docs/decisions.tsv` → +9 rows (4 P1/P2 backfill + 5 session clusters)

## Leftovers

- Sentry/crash reporting, capability matrix script (`scripts/compare_capabilities.py` still stub), AND/OR logical group editor UI — v1.0 gate items from review-swarm Dim 11, not started.
- On-device leak tests + push/tag CI still blocked on external resources (device, git remote).
- flutter_secure_storage still declared-but-unused (plaintext-at-rest is the documented baseline; backup encrypts in transit/at-rest-of-file).
- `l10n.yaml` deprecation warning (synthetic-package) — trivial cleanup.
- Android geo-cache/update-store paths land in systemTemp (env vars unset on Android) — path_provider promotion is a deliberate cross-store decision, not taken this session.
