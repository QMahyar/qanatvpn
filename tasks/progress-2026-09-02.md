# Progress — 2026-09-02

## Session 8 — Engine runtime start (open finding #1 from handoff.md)

### Done
- **Discovery:** the fork's libbox 1.14 aar has **no `Box` class** (the handoff plan's assumption was outdated). The 1.14 daemon architecture exposes `CommandServer.startOrReloadService(config)` and the TUN fd comes from Go calling **back** into Kotlin via `PlatformInterface.openTun()` during box start.
- **Architecture shift (documented in decisions.tsv):** establish+protect moved INSIDE `box.start` for engine-managed platforms. Dart guarded sequence is now `grant → battery → foreground → airplane → tor → box.start(config) → firewall`; fd never crosses to Dart on Android.
- **Dart:**
  - `lib/core/services/tunnel.dart`: `PlatformAdapter.engineManagedTun` getter; connect() skips establish/protect when true; fd close paths null-guarded; `ConfigSource.resolve` now async.
  - `lib/core/services/channel_adapters.dart` (new): `MethodChannelBoxAdapter` (boxStart/boxStop, events stream), `MethodChannelForegroundAdapter`, `LocalSocksTorAdapter` (127.0.0.1:9050 probe).
  - `lib/modules/vpn/repositories/profile_config_source.dart` (new): loads vendored `profiles/config.wg-awg.json` asset; `TOR-` prefix tags set requiresTor.
  - `lib/modules/vpn/logic/vpn_notifier.dart`: added foreground/box/firewall/tor/configSource providers + `tunnelAssemblyProvider`.
  - `lib/main.dart`: full real-adapter wiring (tunnelProvider no longer throws at runtime).
  - `pubspec.yaml`: profiles asset bundled.
- **Kotlin:**
  - `BoxEngine.kt` (new): `Libbox.setup` (filesDir/working/temp, fixAndroidStack, unix-socket command server) + `CommandServer` + `PlatformInterfaceWrapper` (openTun builds VpnService.Builder from TunOptions incl. addresses/routes/dns/per-app include-exclude, establishes, returns fd; autoDetectInterfaceControl → protect; ConnectivityManager default-interface monitor) + `CommandServerHandler`. Engine work on single background executor; channel results posted to main thread.
  - `YourVpnService.kt`: legacy `establishFromParams` + push/pop establish callback queue **removed** (also fixes handoff finding #2 races: orphaned fd + first-Result-dropped).
  - `VpnServiceBridge.kt`: added `boxStart`/`boxStop`; removed dead `establish`/`protect` handlers.
  - Manifest: `ACCESS_NETWORK_STATE`.
- **Verify:** 99 tests green (+3 engine-managed TUN tests: happy path skips fd steps, start-fail no closeFd, disconnect reverses without fd steps), `flutter analyze` clean, `flutter build apk --debug` green (Kotlin compiles against aar).

### Key facts for next session
- `boxStart` channel = blocking call that returns only when `startOrReloadService` completes (config parse + tun establish + instance.Start). Failures arrive as `box_start_failed` PlatformException → Dart blocks with `boxStartFailed`.
- TUN PFD ownership: `PlatformInterfaceWrapper.tunPfd`, closed on boxStop (`closeTun`). Go dups the fd internally.
- Per-app allowlist/bypass reaches the engine via `OverrideOptions.includePackage/excludePackage` (wire wizard step 3 here later) or via tun options.
- Profiles: only the vendored `config.wg-awg.json` (placeholder keys) ships; real endpoints land with the groups/repository work.

### Remaining (updated open findings)
1. **On-device runtime proof:** install APK, grant permission, tap connect — expect Connected + tun0 + proxied egress. Placeholder keys in the profile mean handshake will fail (expected); engine start/stop lifecycle is what's being proven.
2. **Crash events:** box crash mid-run isn't detected yet (needs CommandClient subscribe or status polling); `BoxEvent.crashed` path exists and is tested with fakes.
3. **Kotlin bridge races:** establish-callback queue findings fixed by removal; `VpnService.protect` failure path returns error via `autoDetectInterfaceControl`.
4. Updates→UI wiring (handoff #3), AWG Id/Ip/Ib + IPv6 endpoints (#4), routing depth 24/30 (#5), UI polish (#6) — unchanged.

### Deferrals noted this session
- Windows `BoxAdapter` (sing-box.exe subprocess) — seam ready (`engineManagedTun` false on Windows), not built.
- CommandClient status/log subscription for live box events.
- FirewallAdapter real iptables/WFP enforcement (engine rules carry the policy for now).

---

## Session 8b — Updates→UI wiring + AWG gaps (handoff findings #3, #4)

### Updates→UI (finding #3)
- `lib/modules/updates/updates_controller.dart` (new): `UpdateState` sealed union (Idle/Checking/Available/UpToDate/Failed) + `UpdateController` riverpod Notifier; renders stored result instantly, `checkNow()` refreshes.
- `lib/modules/updates/updates_screen.dart` (new): state card + changelog + install. Install: Android → `ACTION_VIEW` intent via `installUpdate` channel method (VpnServiceBridge), Windows → `explorer <url>`. No new deps (url_launcher avoided per AGENTS ask-first).
- `lib/modules/updates/updater.dart`: added `UpdateSource` (API → latest.json mirror fallback), `resolvePlatformKey()` (ABI-aware), `UpdateStore` (JSON file both isolates read), `plainFetch`, `updateCheckBackgroundTask` (`@pragma('vm:entry-point')`).
- `lib/main.dart`: workmanager `initialize` + `registerPeriodicTask` 24h, best-effort (never blocks start).
- Home: `UpdateTile` (highlights when update available) + `/updates` route.
- l10n: 4 new keys EN/FA (gen-l10n ran).
- **CI single-producer fix:** build-android `metadata` job now the ONLY latest.json writer — polls up to 40min for the windows asset, builds the merged platform map from the release's real asset list, uploads with `--clobber`. build-windows writes only zip + sha256-windows.json. gh-pages downloads latest.json from the release (repo-root file was never committed).

### AWG gaps (finding #4)
- **Probed the real engine:** `endpoints[].id` → `FATAL decode config: unknown field "id"`. The fork (amneziawg-go 3.1) has no Id/Ip/Ib — those are WireSock masquerade keys. But `i1` IS accepted (probe exit 0).
- `AwgValues.toEngineJson()` (new): Jc/Jmin/Jmax/S1-S4/H1-H4/I1-I5 only — Id/Ip/Ib never reach engine JSON. `toJson()` keeps them for editor round-trips.
- `awg_config.dart`: Id/Ip/Ib validation (hex ≤16 chars or decimal, errors-all-at-once); IPv6-safe `peerHost`/`peerPort` (`[2001:db8::1]:51820` → host/port); endpoint validate rejects bare multi-colon v6 (UAPI-ambiguous, needs brackets).
- INI parser: `ID`/`IP`/`IB` keys parsed into awg values.

### Verify
- **115 tests green** (+16: 6 updater store/source, 5 masquerade, 2 IPv6 endpoint, 1 v6 real sing-box check, 1 Id/Ip/Ib no-emit, 1 INI masquerade round-trip), `flutter analyze` clean, `flutter build apk --debug` green, YAML parses, real sing-box probes exit 0 (id FATAL confirmed, i1+v6 config passes).

### Remaining (unchanged)
1. On-device engine start proof; crash-during-run events (CommandClient subscribe).
2. AWG: URL/Clash amnezia parsers already carry Id/Ip/Ib only where schemas allow; `AwgConfig` still single-peer (multi-peer editing lands with groups).
3. Routing depth (24/30 fields, groups 3-tier), UI polish (nav labels, TextScaler, ReduceMotion), Windows BoxAdapter.

---

## Session 8c — Routing depth 30/30 + groups 3-tier + TOR-CHAIN seam (handoff finding #2)

### Fields (verified against fork `option/rule.go`, not guessed)
- Added the 6 missing RouteRule fields: `ipVersion` (enum 4/6), `portRanges`/`sourcePortRanges` (`min:max` spans, validated ints + min<max ≤65535), `processPaths`/`processPathRegexes`, `userIds` (int32), `ipIsPrivate`/`sourceIpIsPrivate` (bools). Compiler emits fork-native shapes. **30/30 fields complete.**

### Groups 3-tier (auto→selector→endpoint)
- `OutboundGroup.selector` (default member, interrupt_exist_connections) / `.urlTest` (url, interval → `5m` string, tolerance) per probed fork shapes.
- Member-ref validation all-at-once: unknown member / duplicate tag / empty members / bad default each surface separately. Known universe = groups ∪ leafOutbounds ∪ {DIRECT, TOR-CHAIN when torChain set}.
- Assembler: group outbounds emitted before the envelope; `PROXY` selector now aggregates DIRECT + all group tags + endpoint tag (never itself).

### TOR-CHAIN seam
- **Probed:** 1.14 has no `chain` outbound (FATAL). Chaining = `DialerOptions.detour`. Endpoint with `detour: tor-entry` passes real check.
- `TorChainOptions` (127.0.0.1:9050, socks v5) → compiler emits sidecar outbound; assembler injects `detour` into an endpoint tagged `TOR-CHAIN` (endpoint's own detour wins); referencing TOR-CHAIN without torChain = validation error. Runtime gating stays with Tunnel's TorAdapter (socks probe before start).

### Verify
- **123 tests green** (+8 routing: 6-field shapes, bad-values all-at-once, group shapes, group errors ×4, TOR-CHAIN error/emit/detour-with-real-check, 3-tier real sing-box check), analyze clean, apk green.

### Remaining
1. On-device engine start proof; crash-during-run events (CommandClient subscribe).
2. UI polish (nav labels, TextScaler clamp, ReduceMotion, M3E goldens), Windows BoxAdapter, wizard per-app → OverrideOptions include/excludePackage.

---

## Session 8d — UI polish pass (handoff finding #2 partial: labels/scaler/motion)

- **Nav labels l10n:** `groups/rules/logs/diagnostics` keys added EN+FA; `AppShell._label` uses l10n getters (no hardcoded strings left).
- **blockReason human strings:** `tunnel_l10n.dart` — `TunnelStateL10n` + `TunnelBlockReasonL10n` extensions; StateTile shows "Airplane mode is on" / "حالت هواپیما روشن است", never `boxStartFailed`. PowerTile state strings l10n'd too; Semantics labels use `l10n.connect/disconnect`.
- **TextScaler clamp:** `clampTextScaler()` in app.dart, applied app-wide via MaterialApp.builder — 0.9 floor / 1.35 ceiling (bento tiles stop overflowing at 200%).
- **ReduceMotion guard:** `MediaQuery.disableAnimationsOf` → power-tile rotation `Duration.zero` (vestibular safety).
- Tests: 5 new (`test/ui/polish_test.dart` — clamp window + boundary, no-enum-leak for all states/reasons, FA strings distinct).
- **128 tests green, analyze clean, apk green.** M3E 200% goldens still deferred (needs golden infra).

### Remaining
1. On-device engine start proof (no adb device attached this session); crash-during-run events (CommandClient subscribe).
2. Windows BoxAdapter; wizard per-app → OverrideOptions include/excludePackage; groups editor screen; M3E goldens.

---

## Session 8e — Windows BoxAdapter + wizard per-app → OverrideOptions (handoff findings #2, #3)

### Windows engine path (finding #2)
- `lib/core/services/windows_box_process.dart` (new): `WindowsBoxProcessAdapter` — writes config to temp file, spawns `sing-box.exe run -c <file> --disable-color`, early-exit detection (exitCode future race at 300ms → StateError with captured FATAL line), crash/stopped events from exit watcher, stop = taskkill (graceful → /F) + `process.kill()` backstop + 5s timeout so wedged processes never hang disconnect.
- `lib/core/services/desktop_platform_adapter.dart` (new): Windows PlatformAdapter — permission trivially granted, no airplane API, `engineManagedTun=true` (exe opens its own wintun; same openTun path as Android, no Dart-side establish).
- main.dart: platform-switched adapters (Windows → DesktopPlatformAdapter + WindowsBoxProcessAdapter; Android → channel pair unchanged).
- **Real-exe smoke test:** spawns, stays alive, dies on taskkill. 6 fake-process lifecycle tests (spawn args, early death → error, spawn failure, double-start, stop no-op, crash event).

### Wizard per-app → engine (finding #3)
- `InstalledApp` + `PlatformAdapter.listInstalledApps()`; Kotlin `listInstalledApps` returns user-installed launchable apps (system packages + self excluded).
- `SplitStore` (`lib/modules/onboarding/split_store.dart`): file-backed SplitChoice {allowMode, packages}; wizard `finish()` persists (empty selection = whole-device, store cleared).
- `ProfileConfigSource` merges the stored choice into every TypedConfig: allowlist → `includePackages`, bypass → `excludePackages` (never both).
- Dart `boxStart` carries include/excludePackages → Kotlin `BoxEngine.boxStart` builds `OverrideOptions` with `StringListIterator`s → Go `StartOrReloadService`.
- `_SplitStep` is now a real picker: loads installed apps async, ListView of CheckboxListTiles, empty/loading states.
- 13 new tests (SplitStore round-trip/corrupt, config-source merge allowlist/bypass/none/TOR, wizard finish persistence, 6 windows-lifecycle).

### Verify
- **141 tests green, analyze clean, apk green, real sing-box.exe spawn/kill smoke passes.**

### Remaining
1. On-device engine start proof (needs device); CommandClient crash events.
2. Groups editor screen; M3E 200% goldens; Windows per-app split (engine-level filtering is Android-only — documented, ships via routing editor rules).

---

## Session 8f — Groups editor + persistence + config injection (handoff finding #2)

- `lib/modules/routing/policy_store.dart` (new): `PolicyStore` + `PolicyDocument` — file-backed `{groups, leafOutbounds}`, selector/urltest round-trip, corrupt file → empty (never throws).
- `lib/modules/routing/groups_controller.dart` (new): `GroupsController` CRUD; every mutation revalidates through the **real RoutingCompiler**, so the screen shows exactly the errors the engine would FATAL on. Persisted even when invalid (editor shows them; connect ignores them).
- `lib/modules/routing/groups_screen.dart` (new): list + add/edit bottom-sheet form (selector/urltest segment, tag, member checkboxes from the known tag universe, default-member dropdown) + delete + live validation error card. `/groups` tab now renders it.
- `ProfileConfigSource`: valid stored groups injected ahead of the envelope selector; `PROXY` member list prepends group tags; **broken policy ships the unmodified profile** (never FATALs the engine — same errors visible in the editor). Injection patches fresh from cache each resolve; base never mutated.
- main.dart wires `policyStoreProvider` into the config source.
- 7 new tests (store round-trip/corrupt, injection valid/broken/absent + non-mutation, controller add-bad-member + delete).
- **148 tests green, analyze clean, apk green.**

### Remaining
1. On-device engine start proof (needs device); CommandClient crash events.
2. M3E 200% goldens; Windows per-app split (via routing process_path rules); endpoint/leaf management UI (policy doc already carries leafOutbounds).

---

## Session 8g — Endpoint import → store → engine config (handoff finding #4: leafOutbounds writer)

### Probed shapes (real sing-box, exit 0 on all 6)
- vless: reality needs `utls` alongside (`uTLS is required by reality client` FATAL otherwise); vmess tls/transport siblings; tuic **alpn under `tls.alpn`** (top-level `alpn` FATALs); hysteria2 salamander obfs + h3; ss plugin_opts `k=v;k=v`; trojan tls with insecure.

### Landed
- `ingestion/endpoint_outbound.dart`: `endpointToOutboundJson(NormalizedEndpoint)` — one emitter per variant; WG/AWG rejected (ships via `endpoints[]`).
- `normalized_endpoint.dart`: `normalizedToJson`/`normalizedFromJson` — store-facing sealed-union serialization, tolerant of schema evolution.
- `endpoint_store.dart`: `EndpointStore` (file-backed, corrupt→empty) + `StoredEndpoint{endpoint, label, sourceUrl}` + `tags()`/`outboundJson()`.
- `endpoints_controller.dart`: `importText` — URL-looking input fetched via shared `plainFetch` (moved to `core/network/plain_fetch.dart`), else parsed raw; dedupe by tag keeps latest; delete persists.
- `endpoints_screen.dart`: import box + list with per-protocol icons; `/endpoints` route; home EndpointTile shows count/first tag and opens it.
- `ProfileConfigSource._merged`: stored endpoint outbounds + group outbounds injected ahead of envelope selector; selector member list gains endpoint + group tags; validation includes endpoint tags as leaves; broken policy ships unmodified profile.
- main.dart wires `EndpointStore` into the config source.
- 8 new tests (per-protocol JSON shapes, all-6 real check, WG rejection, store round-trip/corrupt).

### Verify
- **156 tests green, analyze clean, apk green.**

### Remaining
1. On-device engine start proof (needs device); CommandClient crash events.
2. M3E 200% goldens; Windows per-app split (via routing process_path rules).

---

## Session 8h — Three back-to-back: tag selection, crash events, sentinel fix

### 1. Connect tag selection
- `lib/modules/vpn/logic/selected_endpoint.dart`: `selectedEndpointProvider` — first group's default member/first member → first stored endpoint → vendored profile tag (`HKG-02` fallback). PowerTile connects with the resolved tag; endpoint tile shows it + selection source (`connect: group|endpoint|fallback`).

### 2. CommandClient crash events (mid-run engine death)
- Exported API reality: `CommandClientOptions` supports only 6 commands — **no service-status subscribe** (FATAL status exists only inside the daemon). The sanctioned path is the log stream.
- Kotlin `BoxEngine`: log watcher (`CommandClient` + `CommandLog`) attaches after successful start; `WriteLogs` scanning FATAL while running → `crashed` event; `Disconnected` while running → runtime death → `crashed`. Detached on stop. Start-time FATALs stay synchronous (startOrReloadService error).
- Events cross to Dart on a new `box_events` channel (`onEngineEvent`); `MethodChannelBoxAdapter` fans them into its existing `BoxEvent` stream — Tunnel's `box crash while connected → blocked + firewall` path now fires for real engine deaths.

### 3. Clash mixed-listener sentinel fix
- `listeners: [{type: mixed}]` is a LOCAL inbound — mapping it to `VmessEndpoint(uuid: 'local')` shipped a useless 127.0.0.1 dial into engine configs. Parser now skips it outright; regression test asserts no sentinel leaks. Dead `_listener` method removed.

### Tests
- 4 new: 3 selected-endpoint resolution order, 1 listener-skip. (Crash path exercised via Kotlin compile + existing fake-driven Tunnel tests; real FATAL firing needs device.)
- **160 tests green, analyze clean, apk green.**

### Remaining
1. On-device engine start proof (needs device) — now includes crash-event verification.
2. M3E 200% goldens; Windows per-app split (via routing process_path rules).

---

## Session 8i — Loop: logs tab, rules tab, goldens infra, windows smoke (no device available)

### Logs tab live
- Kotlin: log watcher forwards **every** engine line to Dart (`log` events, not just FATAL scans).
- `lib/modules/logs/log_bus.dart`: `LogBus` (500-line ring + broadcast) + `EngineLogLine.parse` (strips `INFO[0000]`-style prefixes, flags ERROR/FATAL) + `LogsController`.
- `logs_screen.dart`: live log view (monospace, errors colored, clear button). Wired to `/logs`; main.dart shares one LogBus between the box adapter and the provider.

### Rules tab live
- `rule_store.dart`: typed RouteRule JSON persistence + `routingCompilerForRules` gate.
- `rules_controller.dart` + `rules_screen.dart`: CRUD editor (outbound dropdown PROXY/DIRECT/BLOCK/TOR-CHAIN, suffixes/IPs/ports/processes, invert), live compiler validation card, per-rule summaries.
- Config-source merge: user rules append **after** profile rules — firewall-first ordering preserved; invalid rule sets ship the profile unmodified.
- `/rules` now renders the real editor (tab placeholder retired).

### Golden infra (zero new deps — flutter_test matchesGoldenFile)
- Overflow-probe test at pre-clamp 2.0 scale **caught a real bug**: EndpointTile's 3-row column overflowed 3.5px at user-visible scale → fixed with SingleChildScrollView.
- 2 power-tile PNG goldens at 1.35 ceiling (regenerate via `flutter test --update-goldens test/golden`).
- Note: both state goldens currently hash identical (tile is phase-parametric in test scope); distinct-state goldens when tile internals diverge.

### Windows real-exe smoke
- `windows_real_exe_smoke_test.dart` runs the production adapter against the real exe (spawn → started → stop → stopped, TUN-free config, no admin); self-skips when the exe is absent → runs green in windows CI right after `go build`, no-op on ubuntu.

### Verify
- **174 tests green, analyze clean, apk green.**

### Loop status: nothing actionable left without a device.
Remaining: on-device proof (plug device), CI first real run (needs push+tag), website polish.

---

## Session 8j — Loop round 2: Diagnostics tab live (last placeholder retired)

- `diagnostics_controller.dart`: auto-ticks Health.snapshot() every 10s while listened; probe failure keeps last report; `refreshNow()`.
- `diagnostics_screen.dart`: 0-6 score card (level name, colored) + probe tiles (DNS hijack FakeIP probe, TCP ping 1.1.1.1:443, stability window %).
- `/diagnostics` wired; **tabs.dart deleted** — every tab is now a real screen.
- 2 controller tests (immediate snapshot + manual refresh, failure-keeps-last-report).
- **176 tests green, analyze clean, apk green.**

### Loop exhausted — remaining items all need external resources:
1. On-device proof (needs adb device).
2. CI first run (needs push + tag — user hasn't requested commits).
3. Website polish (cosmetic; docs/site exists).
4. Advanced rules-editor fields (compiler supports all 30; UI additions possible but diminishing returns without user direction).
