# Progress 2026-09-03 — production hardening (audit → P0 fixes)

## Audit
- Ran `prod-audit` workflow (9 dimensions → 123 raw → adversarial 2-of-3 vote → **39 confirmed**). ~102 verify agents hit gateway 502s late in the run; synthesis still completed. Full result: Temp `tasks/ww2cioc02.output` + journal `subagents/workflows/wf_61b3efcd-dd3/journal.jsonl`.

## Fixed (P0, all verified)
1. **Engine** (`tunnel.dart`, `channel_adapters.dart`, `vpn_notifier.dart`, `main.dart`, `firewall.dart`): `TunnelTimeouts` per-step ceilings + epoch check after every await; crash-during-connecting now blocks (epoch bump + blocked guard); channel 30/10s timeouts + JSON-encode guard; `PolicyFirewallAdapter` validates hijack-first ordering fail-closed (native OS lockdown documented as gap); notifier disposes sub. 5 new tunnel tests.
2. **Routing** (`routing_compiler.dart`, `routing_policy.dart`, `rule_store.dart`, `profile_config_source.dart`): port 0-65535, CIDR, regex, network/protocol/clash allowlists; SRS derived+explicit merge + registry validation; outbound-ref check incl. `policy.defaultOutbound`; logical shape errors both directions; RuleStore v2 (all 30 fields + nested rules, migration-tolerant); merge per-group fallback + deep-copy + WG tags in selector. 8 compiler tests + v2 round-trip test.
3. **Ingestion/build/observability**: `decodeBase64Flex` iterative fallback (was infinite recursion); `Isolate.run` parse >64KB + `primeCache` + 32-entry cap; Windows both pipes → LogBus (4KB cap); `pubspec.lock` un-ignored; CI android-37; APK 120MB size gate; all exe tests `markTestSkipped` (awg fixed to absolute path).

## Verify
- `flutter test --no-pub`: **194 all pass** (was 180; +14 new, 0 skipped locally)
- `flutter analyze lib/`: No issues (1 info in test/ only — `prefer_const_constructors` on `RuleDocument(`)
- `sing-box check -c profiles/config.wg-awg.json`: exit 0
- `docs/decisions.tsv`: +14 rows

## Leftovers (P1/P2, not started)
- Config rebuild caching (hash inputs), store write coalescing (debounce + atomic), startup parallelize + Stopwatch, connect-latency metrics, log level filter/search/export, l10n hard-coded strings, TextScaler 2.0 goldens, rules editor advanced fields (process_path/user_id UI), DNS fake-ip-filter (fork schema lacks it — documented), sentry/crash reporting, per-ABI splits.
- Next session: pick P1 perf (config cache + coalescing) or push/CI (`git remote add` → tag `v0.1.0` → verify latest.json) + on-device proof. `pubspec.lock` is untracked — `git add` it on next commit.
