# Flutter
.dart_tool/
.buildlog/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
# pubspec.lock pins the shipped dependency set — keep it tracked for
# reproducible builds. Tool-local lockfiles stay ignored below.
!pubspec.lock
*.log.lock

# Go
android/app/libs/libbox.aar
android/app/libs/libbox-legacy.aar
windows/libbox.dll

# Secrets
android/key.properties
android/app/upload-keystore.jks
*.jks
*.p12

# Hallmark cache
.hallmark/cache/

# IDE
.idea/
.vscode/
*.iml
# Build artifacts
android/.gradle/
go/amnezia-box/build/
windows/sing-box.exe
profiles/cache/
latest.json
coverage/
doc/api/
*.log

# agent session scratch
.pi/
thirdparty_rows.json
